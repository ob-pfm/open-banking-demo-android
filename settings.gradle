dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        maven {
            url '/Users/erickdominguez/.m2/repository'
        }
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}

include ':app'
